// step 1 object structure for a single task
const task = {
    id: 'task-1',
    title: 'Work on my portfolio.',
    isCompleted: false,
    createdAt: new Date(),
    dueAt: null // for reminders
}
// step 2 to store the tasks in local storage
 function getTasks(){
    const stored = localStorage.getItem('tasks');
    if{
        stored() = null;
    }
 }